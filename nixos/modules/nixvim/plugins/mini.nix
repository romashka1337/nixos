{
  programs.nixvim.plugins.mini = {
    enable = true;

    modules = {
      # align = {};
      # basics = {
      #   options.extra_ui = true;
      # };
      # clue = {};
      # cursorword = {
      #   enable = false;
      # };
      # doc = {};
      # extra = {};
      indentscope = {};
      # trailspace = {};
      # misc = {};
      # surround = {};
    };
  };
}
